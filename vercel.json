{
  "version": 2,
  "builds": [
    {
      "src": "./backend",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/backend/index.js"
    }
  ],
  "build": {
    "commands": [
      "cd backend",
      "npm install",
      "npm run build"
    ]
  }
}
